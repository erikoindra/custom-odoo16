<odoo>
    <!-- Inherit Account Tax view form -->
    <record id="view_tax_form" model="ir.ui.view">
        <field name="name">account.tax.form.inherit.account_postponed_vat</field>
        <field name="model">account.tax</field>
        <field name="inherit_id" ref="account.view_tax_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='country_id']" position="after">
                <field name="is_postponed_vat" attrs="{'invisible': [('type_tax_use', '!=', 'purchase')]}"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit Account Tax view tree -->
    <record id="view_tax_tree" model="ir.ui.view">
        <field name="name">account.tax.tree.inherit.account_postponed_vat</field>
        <field name="model">account.tax</field>
        <field name="inherit_id" ref="account.view_tax_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[last()]" position="after">
                <field name="is_postponed_vat" optional="hide" widget="boolean_toggle"/>
            </xpath>
        </field>
    </record>
</odoo>